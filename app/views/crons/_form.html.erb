<%= form_with(model: cron, local: true) do |form| %>
  <% if cron.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cron.errors.count, "error") %> prohibited this cron from being saved:</h2>

      <ul>
      <% cron.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :cron_name %>
  </div>

  <div class="field">
    <%= form.label :unique_id %>
    <%= form.text_field :unique_id, id: :cron_unique_id %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url, id: :cron_url %>
  </div>

  <div class="field">
    <%= form.label :expression %>
    <%= form.text_field :expression, id: :cron_expression %>
  </div>

  <div class="field">
    <%= form.label :grace_period %>
    <%= form.number_field :grace_period, id: :cron_grace_period %>
  </div>

  <div class="field">
    <%= form.label :enabled %>
    <%= form.check_box :enabled, id: :cron_enabled %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
